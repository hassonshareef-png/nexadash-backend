services:
  - type: web
      name: nexadash-backend
          runtime: python
              buildCommand: pip install -r requirements.txt
                  startCommand: gunicorn main:app --bind 0.0.0.0:$PORT
                      healthCheckPath: /health
                          envVars:
                                - key: SECRET_KEY
                                        generateValue: true
